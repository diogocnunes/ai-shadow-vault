#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LIBEXEC_DIR="$(cd "$SCRIPT_DIR/../libexec/ai-vault" && pwd)"

echo "vault-init is kept for compatibility. Prefer: ai-vault init" >&2
exec "$LIBEXEC_DIR/bin/ai-vault" init "$@"
